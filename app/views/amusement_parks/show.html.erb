<h1><%= @amusement_park.name %></h1>
<h2>Admissions: $<%= "#{@amusement_park.admission_price}" %></h2>

<section class ='rides'>
  <ol>
  <% @sorted_rides.each do |ride| %>
  <li><%= ride.name %></li>
  <% end %>
  </ol>
</section>

<section class='thrill_rating'>
  <p>Average Thrill Rating of Rides: <%= "#{@amusement_park.average_thrills}" %>/10</p>
</section>
